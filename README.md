# 🚀 Инструкция по запуску EGPower Clicker

### 1. Подготовка Telegram Бота
1. Создайте бота в [@BotFather](https://t.me/BotFather) через `/newbot`.
2. Создайте приложение через `/newapp`, выбрав вашего бота.
3. В поле `Web App URL` укажите ссылку на ваш будущий сайт (например, от Vercel).
4. Получите API токен бота.

### 2. Настройка Бэкенда (Node.js)
Бэкенд нужен для обработки команд бота и (в будущем) сохранения данных в базу.
1. Установите зависимости в папке `backend`: `npm install`.
2. Создайте файл `.env` в папке `backend`:
   ```env
   BOT_TOKEN=ваш_токен_из_BotFather
   WEB_APP_URL=ссылка_на_ваш_сайт
   PORT=3000
   ```
3. Запустите: `node index.js`.

### 3. Настройка Фронтенда (React)
1. Убедитесь, что в `config.ts` указан правильный `botUsername` вашего бота.
2. Соберите проект и разместите на хостинге (Vercel/Netlify/GitHub Pages).
3. Ссылка на этот хостинг должна совпадать с тем, что вы указали в BotFather.

### 4. Основные функции
- **Clicker**: 3D монета с реалистичной физикой.
- **Mining**: Покупка бизнесов для пассивного дохода.
- **Wheel**: Ежедневное колесо удачи.
- **Wallet**: Вывод через менеджера (кнопка связи).
- **AI Builder**: Генерация новых рынков через Gemini AI.

---
**Разработчик:** EGPower Team
**Технологии:** React, Three.js, Node.js, Telegram WebApp API.
